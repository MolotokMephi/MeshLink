name: Android

# One trigger pair, deduped via concurrency: branches with an open PR
# only run once (the pull_request event), main pushes still build.
on:
  push:
    branches: [main]
    paths:
      - "android/**"
      - ".github/workflows/android.yml"
  pull_request:
    paths:
      - "android/**"
      - ".github/workflows/android.yml"

# Cancel earlier runs of the same workflow on the same ref. Pushing
# multiple commits in quick succession only keeps the newest run alive.
concurrency:
  group: android-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  build:
    name: Build & lint
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Cache Gradle
        uses: actions/cache@v5
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('android/**/*.gradle*', 'android/**/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-${{ runner.os }}-

      - name: Probe runner toolchain
        run: |
          set -eux
          java -version 2>&1 || true
          # ubuntu-latest images ship with the Android SDK pre-installed
          # at /usr/local/lib/android/sdk; surface the exact paths so the
          # gradle build step picks them up via ANDROID_HOME below.
          echo "ANDROID_HOME=${ANDROID_HOME:-}"
          echo "ANDROID_SDK_ROOT=${ANDROID_SDK_ROOT:-}"
          ls "${ANDROID_HOME:-/usr/local/lib/android/sdk}" || true

      - name: Assemble debug APK
        id: assemble
        working-directory: android
        run: |
          set -eo pipefail
          chmod +x ./gradlew
          ./gradlew --version
          ./gradlew :app:assembleDebug --stacktrace 2>&1 | tee /tmp/gradle.log

      - name: Surface failure to step summary + PR comment
        if: failure() && steps.assemble.conclusion == 'failure'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          {
            echo "# Build failure"
            echo
            echo "## Tail of gradle output"
            echo '```'
            tail -200 /tmp/gradle.log 2>/dev/null || echo "no gradle log"
            echo '```'
          } > /tmp/summary.md
          cat /tmp/summary.md >> "$GITHUB_STEP_SUMMARY" || true
          if [ -n "${PR_NUMBER:-}" ]; then
            jq -n --arg body "$(cat /tmp/summary.md)" '{body: $body}' > /tmp/comment.json
            curl -sS -X POST \
              -H "Authorization: token $GH_TOKEN" \
              -H "Accept: application/vnd.github+json" \
              -d @/tmp/comment.json \
              "https://api.github.com/repos/${{ github.repository }}/issues/${PR_NUMBER}/comments" \
              -o /tmp/comment_response.json -w "HTTP %{http_code}\n" || true
          fi

      - name: Run unit tests
        working-directory: android
        run: ./gradlew :app:testDebugUnitTest --stacktrace
        continue-on-error: true

      - name: Upload APK
        if: success()
        uses: actions/upload-artifact@v7
        with:
          name: meshlink-debug-apk
          path: android/app/build/outputs/apk/debug/*.apk
          if-no-files-found: ignore
