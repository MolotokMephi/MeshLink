<?xml version="1.0" encoding="utf-8"?>
<!-- Identity keys never leave the device, so cloud + device-to-device
     transfers exclude every shared-prefs and database that holds them.
     The user's recovery phrase is the only supported backup path. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
