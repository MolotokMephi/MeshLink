<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash/launcher theme. The actual UI is rendered by Compose under
         MeshLinkTheme, so we just need an action-bar-less host.
         `Theme.DeviceDefault.DayNight` only exists from API 29; we use
         the plain DeviceDefault parent here and override with DayNight in
         values-v29 so the dark-mode wallpaper colour kicks in on current
         hardware while older devices still resolve. -->
    <style name="Theme.MeshLink" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
    </style>
</resources>
